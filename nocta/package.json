{
  "name": "nocta",
  "version": "1.0.0",
  "private": true,
  "description": "NOCTA - Night Commerce OS for Swiss drinks & snacks delivery",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server",
    "test": "npm run test --prefix server",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix web",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "@playwright/test": "^1.49.1"
  }
}
